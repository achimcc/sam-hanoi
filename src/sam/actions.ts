const actions: Actions = {
  INIT: (present: Presenter, intent: Intent) => {
    present(intent);
  },
  TILES: (present: Presenter, intent: Intent) => {
    present(intent);
  },
  START: (present: Presenter, intent: Intent) => {
    present(intent);
  },
  DROP: (present: Presenter, intent: Intent) => {
    present(intent);
  },
  SOLVE: (present: Presenter, intent: Intent) => {
    present(intent);
  },
};

export default actions;
